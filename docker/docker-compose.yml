# =============================================================================
#  oh-my-pi（omp 官方版）— docker-compose.yml
#  官方仓库：https://github.com/can1357/oh-my-pi（omp.sh）
#  Usage:
#    docker compose up -d                          # 常驻（exec 进去跑 omp）
#    docker exec -it oh-my-pi omp                  # 交互 TUI
#    docker compose run --rm omp -p "总结这个项目"   # 一次性任务
# =============================================================================
services:
  omp:
    build:
      context: .
      args:
        # 国内加速默认值在 Dockerfile；如需切官方源或换加速站：
        #   docker compose build --build-arg APT_MIRROR=deb.debian.org --build-arg NPM_REGISTRY=https://registry.npmjs.org
        NODE_IMAGE: ${NODE_IMAGE:-node:24-bookworm-slim}
        APT_MIRROR: ${APT_MIRROR:-mirrors.aliyun.com}
        NPM_REGISTRY: ${NPM_REGISTRY:-https://registry.npmmirror.com}
    container_name: oh-my-pi
    environment:
      - TZ=Asia/Shanghai
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      # DeepSeek 官方 provider 凭据（pi 系同源机制，env 注入即被识别）
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY:-}
    volumes:
      # ── 分层只读 v2（2026-09-11，修正大小写别名绕过）──
      # v1 教训：整仓 rw + 子目录 ro 的方向在 9p 大小写不敏感透传下可被
      # 大小写别名绕过（/workspace/SKILLS 落到同一 Windows 目录但错过 ro 挂载点，写入真实落盘）。
      # v2 反转：整仓 :ro 为底，仅工作对象以精确路径 rw 子挂载放行——
      # 任何大小写变体的写入都会落在 ro 父挂载上被拒，护栏不可绕过。
      # 更新路径：改仓库 → CI（G3 哈希守副本一致）→ docker compose up -d 重新生效。
      - ..:/workspace:ro
      - ../architect-knowledge:/workspace/architect-knowledge:rw
      - ../docs:/workspace/docs:rw
      - ./omp:/home/pi/.omp
      - ./entrypoint.sh:/usr/local/bin/omp-entrypoint.sh
    restart: "unless-stopped"
    stdin_open: true
    tty: true
