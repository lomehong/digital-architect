# 走查记录：任务面/记忆面宿主抽象（omp 台账实现）

> 走查人：本建设会话 · 日期：2026-09-11 · 任务：TB-1789106499651-ky01n（主人已批准）
> 背景：主人指出「omp 无法使用 dsh 看板——看板实现按宿主调整，方法论不变」。

## 一、落地内容

| 项 | 产物 |
|---|---|
| 契约 | `architect-knowledge/principle/task-and-memory-surface.md`（任务面五操作 + 四态状态机 + 四不变量；记忆面 write/read/supersede/verify + 两条不变量）——待审核入库 |
| omp 实现 | `scripts/task-ledger.mjs`（new/claim/report/confirm/reject/list/archive + `--validate` + `--selftest`，零依赖） |
| 落点 | **目标项目内 `.architect/tasks/<taskId>.yaml`**（git 管理、跨会话、团队可审计；**零挂载扩权**，故未触发 High-risk 决策门） |
| CI | 新 step：`--selftest`（机制红绿门禁）+ `--validate --root .`（结构校验；无台账目录时跳过并明示） |
| 适配层 | dsh.md / oh-my-pi.md 的「任务执行」「记忆沉淀」两面改为**契约 + 各自实现**对照表 |
| SKILL | 四个 SKILL 中写死的 dsh 工具名改为**任务面操作名**（create/claim/report/confirm），实现映射交由适配层；`architect-design` 第七步补「执行方由主人点名」 |

## 二、验证结果（真实输出）

| # | 项 | 结果 |
|---|---|---|
| 1 | 脚本自测红绿 | ✅ `--selftest` 通过：绿=立项→认领→自报→确认→归档+校验；红=未 claim 即 report / 未 report 即 confirm / 无 `--confirmed-by` / 无 `--confirmed-via` / 未落定即归档 / 已落定后再 claim 或 reject / 损坏台账被 `--validate` 捕获（7 类红态） |
| 2 | **容器内实跑（omp 侧，pi 用户）** | ✅ `new→claim→report→confirm→list` 全链成功；台账文件字段齐（含 `confirmedBy: master` / `confirmedVia: ask#demo` / 完整 events 时间线） |
| 3 | 容器内红态 | ✅ 已落定后再 `claim` → `exit=1`，报「非法跳步：当前「已落定」，不允许 claim；当前允许：（无——已落定，需先 reject 或归档）」 |
| 4 | 副本一致性 | ✅ 5 SKILL × 2 镜像一致 |
| 5 | 知识库 lint | ✅ 26 条目（25 已确认 / 1 待审核）0 错 0 警 |
| 6 | 台账结构校验 | ✅ `--validate` 通过；无台账目录时「跳过并明示」，不静默绿（回应评审扣分项） |

## 三、过程发现

- **F1（评审扣分项已闭合）**：评审判定「`--validate` 对空台账目录的判定口径未写明」→ 实现在输出中**明示跳过原因**并在 CI 中与 `--selftest` 并列（机制门禁 + 结构门禁），不制造「假绿」。
- **F2（发现真实缺口，待办）**：`new` 事件里 `by=unknown`——创建者标识未随 `--by` 传入。属可用性小瑕疵，登记待修（不影响状态机与不变量）。
- **F3（契约与实现一致性）**：dsh 侧看板语义（立项→认领→自报→待确认→落定）与本契约四态**完全同构**，故 SKILL 泛化属「改名不改义」，无需回改存量方案文档。

## 四、未兑现 / 待办

- **U1（登记）**：跨项目任务检索（多项目分散台账的集中视图）未做——先用 `list --root <path>` 枚举；待 3~5 案例后复审判定是否建索引；
- **U2（登记）**：omp 台账与 dsh 看板**不建双向同步**（本次显式不做）；跨宿主接手同一任务的场景出现时另立需求；
- 本任务契约条目 `principle/task-and-memory-surface.md` 待主人确认升级；
- F2（`new` 的 by 标识）待修。
