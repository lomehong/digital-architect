# 走查记录：v3 大脑/现场解耦（omp 系统级知识/工具/skills + workspace 参数化）

> 走查人：本建设会话 · 日期：2026-09-11 · 任务：TB-1789096133862-kfiuu（主人已批准）
> 动机：主人指出 v2 角色错位——大脑仓（digital-architect）不该占据 workspace；workspace 应绑定架构师**实际服务的目标项目**，知识/工具/skills 应成为 omp 系统级（agent 级）资产。

## v3 形态（docker-compose.yml，commit b430760/db3861c 后续）

| 层 | 容器内位置 | 权限 |
|---|---|---|
| 现场 = 目标项目 | `/workspace`（`TARGET_PROJECT` env 参数化，当前绑定 `E:\code\nodejs\dsh`） | rw |
| 大脑仓 | `/opt/architect` | ro |
| 大脑写入面 | `/opt/architect/architect-knowledge`、`/opt/architect/docs` | rw（白名单） |
| 系统级技能 | `/home/pi/.omp/agent/skills`（挂 `skills/` 权威源） | ro |
| 系统级工具 | `/home/pi/.omp/agent/tools`（docker/agent-tools 绝对路径转发 → `/opt/architect/dsh-architect/src/omp.ts`） | ro |

配套：四个 SKILL 头部增「路径基准」注（仓库相对路径相对大脑仓根解析，按适配文件取挂载点）；`adapters/oh-my-pi.md` 挂载面重写。

## 验证结果（全部通过）

| # | 项 | 结果 |
|---|---|---|
| 1 | agent 级技能发现（cwd 任意处，含 /tmp、/、/workspace） | ✅ 四技能 |
| 2 | agent 级工具注册 | ✅ 三检查器在工具面（以 pi 用户） |
| 3 | SKILL 读系统级知识库（/opt/architect/architect-knowledge） | ✅ principle 索引 10 条目实读 |
| 4 | 对目标项目（dsh 仓）真实需求 digest | ✅ 六项覆盖全过，证据横跨 /workspace/dsh-memory 与大脑知识库 |
| 5 | design 自检（真实方案文档） | ✅ 60/60，五问 5/5 |
| 6 | 篡改负向：改 /opt/architect/skills 下 SKILL | ✅ EROFS 拒绝，文件未动 |
| 7 | 知识库写入面 | ✅ probe 写入/清理成功 |
| 8 | 解耦确认 | ✅ /workspace 内无大脑仓任何痕迹 |

## 过程发现（如实登记）

- **F1 执行上下文差异**：`docker exec` 默认 root（agent 目录取 `/root/.omp`，未挂载）→ 工具不注册；真实会话经 entrypoint gosu 成 pi（`/home/pi/.omp`）正常。**纪律：容器内 omp 操作一律 `docker exec -u pi`**。
- **F2 cwd=/workspace 首跑偶发超时**（9p 跨界盘较慢）：重试即恢复；v3 下 cwd 已无关紧要（能力面系统级），推荐会话从目标项目根或 /tmp 启动。
- **F3 digital-twin 仓已不在** `E:\Development\Code\nodejs\digital-twin`（空壳无 .git）——HANDOFF §2.1 路径失真，属知识漂移，待主人确认目标项目清单后回写。
- **F4 SKILL 批量改注事故（已修复）**：PowerShell 反引号转义 + `-replace` 全局匹配致注释重复插入/字符损坏，已全量清理并以 G3 副本校验兜底；教训：跨语言转义场景优先用单引号 here-string 或 .NET API 逐文件精确插入。

## 遗留

- 治理类降级（强制 ask）仍未实测（无人值守模式无交互对象）；
- 演示目标项目 `E:\code\nodejs\omp-walkthrough-target` 可删除；`.env` 的 TARGET_PROJECT 切换真实项目 = 一行 + `docker compose up -d`。
