# 结构化需求包：knowledge-lint 隔离上下文感知（R7 快照假阳性根修）

> 产出者：本会话（dsh 侧，architect-prd-digest 口径）· 日期：2026-09-14
> 来源：KB-R7-LEGACY 裁定（容器 11 处 R7 快照假阳性，判定备案豁免运行中，`docs/designs/2026-09-14-阶段3试点-OPSAUDIT-1-评审记录-dsh侧.md` §KB-R7-LEGACY）+ 容器实证（xd://architect_lint 报 11 处 R7 error；bash 兜底 `ERR_MODULE_NOT_FOUND`）+ 主人指示「开始 lint 根修立项排期」（2026-09-14）
> 关联：`architect-knowledge/practice/node-modules-symlink-cross-host.md`（junction 悬空模式，已确认）；`principle/host-neutral-core.md` 细则 6

---

## 0. 一句话需求

让 knowledge-lint 在「KB 非完整仓上下文」（容器白名单挂载快照）下，对指向 KB 之外大脑仓文件的 ref 不再误报 R7 error——**隔离上下文自动识别并降级为 warning + 备案计数**，完整仓上下文严格语义不变；同时解决容器 bash 兜底路径的 `architect-core` 解析失败（junction 悬空 → 物化真实拷贝）。

## 1. 可验收目标（Acceptance）

| # | 目标 | 验收判据 |
|---|---|---|
| G1 | 完整仓严格语义不变 | dsh 侧（完整大脑仓）R7 维持 error；现有 41 条目全库 `lintKnowledgeAt` 0 错 0 警回归 |
| G2 | 隔离上下文降级 | 容器 KB 挂载（/opt/architect/architect-knowledge，无 .git 父目录、无 docs/）下：KB 外不可达 ref 的 R7 降级 warning + 备案计数输出（形如「N 处 KB 外 ref 未解析（隔离上下文）」）；退出码 pass |
| G3 | 识别可控 | 隔离上下文自动探测 + 显式覆盖（参数/env）；完整仓（父目录有 .git）**绝不误触发**（负向用例红绿） |
| G4 | 容器两路径一致 | `xd://architect_lint` 与 `node dsh-architect/scripts/lint-knowledge.mjs`（bash 兜底）在容器内均运行且结果一致（junction 悬空问题经物化真实拷贝解决） |
| G5 | 备案豁免可撤 | 落地后容器不再依赖「11 处备案豁免」人工口径即可 PASS；备案清单撤销登记 |
| G6 | 主人确认 | 自报 ≠ 完成；落定以主人确认为准 |

## 2. 范围（Do）

- **architect-core**（改）：隔离/快照上下文探测扩展——`lintKnowledgeAt` 在现有 `.snapshot` 标记之外，增「父目录无 .git 且仓根级目录（docs/）缺失」→ 视为隔离上下文（R7 soft + R4/R5 queueLenient 同既有）；显式覆盖选项（opts 或 CLI 参数）；core 单元测试红绿（隔离场景降级 / 完整仓负向不误触发）；
- **dsh-architect**（改）：构建/安装时把 `node_modules/architect-core` **物化为真实拷贝**（弃 junction——对齐 CI 已有物化做法，子仓 8ff370f 先例）；本地宿主执行一次并验证容器解析；
- **omp-architect**（验，零改动预期）：探测在 core 内部做（唯一事实源），外壳透传；若需显式标志补传参；
- 容器实测：两路径 R7 → 11 处 warning 备案（非 error）、退出码 pass；豁免清单撤销；
- 走查记录落 `docs/designs/`。

## 3. 不做项（Don't）

| # | 排除项 | 原因 |
|---|---|---|
| 1 | 不改完整仓 R7 严格语义（error 阻断） | 裁定已明：refs 正确，完整仓须保持严格 |
| 2 | 不动 41 条已确认条目的 ref 内容 | 裁定：改写属降级，不做 |
| 3 | 不动 v3.2 白名单挂载（不给容器 docs/） | 主人拍板的分层只读面，不动 |
| 4 | 不重设计 R1–R9 其他规则 | 范围收敛于上下文感知 |
| 5 | 不做 lint 的其他增强（生产快照宽松语义之外不扩） | 单一主题 |

## 4. 假设

| # | 假设 | 依据 |
|---|---|---|
| A1 | R7 三基准解析（KB 根/仓库根/条目目录）在容器内对 KB 外 ref 必然解析失败 | 容器实证 11 处；kbcollect.ts L136 |
| A2 | 容器 KB 挂载父目录（/opt/architect）无 `.git` 且无 `docs/`——可作隔离上下文判定信号 | 容器挂载布局（v3.2 白名单）；负向：完整仓父目录有 .git |
| A3 | junction 悬空经物化真实拷贝可解 | `practice/node-modules-symlink-cross-host.md`（已确认）；CI 物化先例（8ff370f） |
| A4 | 既有 soft/queueLenient 机制可直接复用，仅缺探测条件 | kbcollect.ts L137/L154-158（实读） |

## 5. 阻断项

**无。**

## 6. 待确认（问主人）

| # | 问题 | 建议 | 状态 |
|---|---|---|---|
| Q1 | 隔离识别方式 | 自动探测（父目录无 .git 或 docs/ 缺失）+ 显式覆盖（`--isolated` / env）双轨 | 待主人拍板 |
| Q2 | 物化落点 | dsh-architect 构建脚本物化 core 为真实拷贝（对齐 CI 8ff370f 同法） | 待主人拍板 |
| Q3 | 双外壳零改动（探测在 core 内部，唯一事实源） | 建议同此（外壳只传参/透传） | 待主人确认 |

## 7. 专项评审触发

- **规则类知识变更**：lint 语义变更属规则类——**必须带真实案例回归**（容器 11 处豁免清单实测即回归）；`practice/suite-build-lessons.md` 教训；
- Compliance / High-risk Change：不命中（纯内部校验工具语义；不扩权限面）；
- Cross-team：不命中（单仓 core + 子模块外壳，属本总仓范围）。

## 8. 六项覆盖检查表

| # | 检查 | 结论 | 证据 |
|---|---|---|---|
| ① 需求覆盖 | ✅ | 裁定移交 + 容器实证逐条归属（G1–G6）；无悬空 | §0/§1 |
| ② 系统覆盖 | ✅ | architect-core（探测+选项+测试）、dsh-architect（物化）、omp-architect（透传验证）、容器（实测）；范围外：dsh-yuyi/套件零改动 | §2 |
| ③ 证据覆盖 | ✅ | 既有机制实读（kbcollect.ts L136-158）；junction 实证（容器回执 readlink）；先例 8ff370f | §4；附 code 行号 |
| ④ 风险覆盖 | ✅ | 兼容（完整仓语义不变+负向用例）/异常（探测误判→显式覆盖兜底）/状态机（无自有状态）/安全（无权限面变化） | §1 G3 |
| ⑤ 验证覆盖 | ✅ | core 单测红绿 + 容器两路径实测 + dsh 完整仓回归 + 撤豁免后自证 | G4/G5 |
| ⑥ 不确定性治理 | ✅ | Q1–Q3 显式挂起未补全 | §6 |

## 9. 五问准入

| # | 问 | 答 |
|---|---|---|
| 1 | 问题与方案匹配 | ✅ 问题找方案：容器 11 处 R7 假阳性 + bash 兜底解析失败均为实测 |
| 2 | 价值依据 | ✅ 备案豁免是人工口径，机制化才可持续；且恢复容器 bash 兜底路径 |
| 3 | 范围受控 | ✅ 五条不做项；改动收敛 core + 一外壳物化 + 一外壳验证 |
| 4 | 复用既有能力 | ✅ soft/queueLenient/快照识别骨架全 Reuse；物化对齐 CI 先例 |
| 5 | 可逆/可回滚 | ✅ git revert 即回滚；容器豁免口径可回退启用 |

## 10. 准入结论

**通过，可进入 architect-design。** 无阻断项；Q1–Q3 属实现形态细节，不阻塞设计（方案中给出建议并标注待拍板）。本文件为自报产出，落定以主人确认为准。
