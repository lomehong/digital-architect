# 走查记录：协同御驿化阶段 1——architect preset 物化（K1=A）

> 日期：2026-09-14 · 执行：本建设会话（方案 `2026-09-11-协同御驿化接线与试点-技术方案.md` 阶段 1，K1=A 独立 dsh profile 已落定）
> 自报 ≠ 完成：本记录待主人确认。

## 已执行

1. **dsh-architect 子模块同步**：本地工作树 `e31df61` → 父仓记录的 `12c6d3e`（v0.3.1，含 architect preset 与版本化物化器，提交 15183e2）；`npm ci + build` 绿。
2. **preset 物化成功**：经 `lib/materialize.js` `materializeAt(DSH_HOME, packageDir)` 直调（与宿主 apply 同一代码路径，幂等版本戳）——
   - 目标：`$DSH_HOME/.agent-presets/architect/`（`agent.cordis.yml` 4303B + `preset.yml` + `.materialized-version`=`1`）；
   - **可选行探测双双命中**（探测面 `$DSH_HOME/profiles/web/node_modules`）：追加 `tool-yuyi`（`dsh-yuyi/tools`）与 `tool-memory`（`@dsh-extra/dsh-memory/tools`）；
   - persona 行 = 最小身份声明；纪律来自项目工作区（AGENTS.md / architect-knowledge）——与本仓设计一致。
3. **独立 profile 脚手架就位**：`$DSH_HOME/profiles/architect/package.json`——bundles = dsh-base + dsh-web-app + `dsh-yuyi`（v0.1.5，与 web profile 同版本）+ `@dsh-extra/dsh-architect`（**v0.3.1**）；release 资产已实测可达（`dsh-architect-latest.tgz`，200/11921B）。
4. **身份事实核对**（物化前置）：本机 web profile 已装全套套件含 dsh-yuyi v0.1.5；observatory `data-roots.yml` 已登记 `dsh-architect-01` → agentId `88efe2d1…`（2026-09-12 真实御符自验，标注「digital-architect（dsh 建设会话）」）；omp 侧 `omp-ops-pi-01` → `5747e738…`。

## 待完成（阶段 1 验收第三条：御符独立发信可见）

- **Q2′（主人）**：architect 实例御符 token 的落法——
  - 路线甲（K1=A 严格形态）：签发**新 token** 注入独立 profile 进程（启动后与 88efe2d1 并列为新 agentId，需同步更新 `data-roots.yml` 映射与心跳 `YUFU_CREDENTIAL`）；
  - 路线乙（最快试点）：本 web 进程身份 `88efe2d1` 即「dsh 建设会话 = 架构师实例」，preset 已在本 host 物化，**建会话即架构师会话**直接参与试点；与 K1=A 的偏离点=不做第二进程（前提：分身侧不共用该 yuyi 身份——主人红线本就只禁分身/架构师共享）。
- 御符 token 由主人提供/注入（明文不进仓）：独立 profile 走进程 env（`YUYI_TOKEN`→dsh-yuyi `tokenEnv`）或 dsh 凭证服务；本会话不经手明文。

## 与方案验收的对照（方案 §7 阶段 1）

| 可验收条目 | 状态 |
|---|---|
| preset 物化成功 | ✅ 本次（stamp=1，幂等可重跑） |
| 工具行就位 | ✅ tool-yuyi + tool-memory 探测追加；architect 四工具随 bundle 自带 |
| 御符独立发信可见 | ⏳ 待 Q2′（token 落法）+ 进程启动 + `yuyi_peers` 互见实测（与 omp-ops-pi-01） |
