# 结构化需求包：架构师 Agent 编码能力（architect-implement + 实现评审）

> 产出者：architect-prd-digest（本会话代跑）· 日期：2026-09-11 · 宿主：dsh（`adapters/dsh.md`）
> 需求来源：主人 2026-09-11「架构师 Agent 也要能直接编码，须遵循开发流程」+ 三问拍板（Q1 主人点名执行方；Q2 首试点=entrypoint fail-fast；Q3 强制 feature 分支）
> 方法论依据：principle/architect-agent-core-methodology.md（三支柱）、principle/executable-design-six-dimensions.md（验证覆盖=先有验收后有代码）、practice/multi-session-git-discipline.md（多会话提交纪律）
> 准入结论：**通过**（Q1~Q3 已闭合，无阻断项）

---

## 1. 可验收目标

1. `skills/architect-implement/SKILL.md` 就位，三副本同步经 `check-skills-sync.mjs` 校验通过，CI 绿；
2. `architect-review` SKILL 增「实现评审」模式（diff+方案为输入，抽查真跑测试）；
3. 知识条目（自编/委派选择规则 + 编码分支纪律）以 `待审核` 入库，review-queue 与 index 同步，lint PASS；
4. `adapters/dsh.md`、`adapters/oh-my-pi.md` 补「编码执行面」五面映射；
5. **首试点自举**：entrypoint fail-fast 改造完成——占位符/空密钥时容器拒绝启动并给出可读提示；正常密钥正常启动；证据为容器实测红绿双态；
6. 全流程文档四件（需求包/方案/评审/走查）落 `docs/designs/`；分项中文提交；
7. 主人确认（自报 ≠ 完成）。

## 2. 范围（Do）

- 新增 implement SKILL（七步：测试清单先行 → feature 分支 → 小步提交 → 每步测试 → 对照自检 → 停止条件 → 自报）；
- review SKILL 增实现评审模式；两份适配文件补编码执行面；
- 知识库：新增 principle/practice 条目（主人点名制 + 分支纪律），走待审核流程；
- 试点编码：`docker/entrypoint.sh` fail-fast + feature 分支实操全过程；
- 文档：需求包/技术方案/评审结论/走查记录。

## 3. 不做项（Don't）

- **不改六维度模板**（实现质量由 implement SKILL 的对照自检承载，避免模板膨胀成万能表单）；
- **不做 service-knowledge 量化**（独立短板，另行立项）；
- **不制定自动自编/委派判定规则**（Q1：主人点名制；规则待案例积累后另议）；
- **不碰 push/部署权限语义**（仍按 L2+ 主人审批；omp 侧降级=强制 ask）；
- **不改 dsh 侧分身执行机制**（架构师自编不替代看板执行轨道）。

## 4. 假设

| # | 假设 | 依据 |
|---|---|---|
| A1 | 目标项目 workspace 已是 rw，架构师可在其内建 feature 分支编码 | v3 compose（TARGET_PROJECT rw）实测 |
| A2 | feature 分支在目标项目内操作；本仓试点时即在 digital-architect 内建分支 | Q3 + git 惯例 |
| A3 | 「测试清单先行」可由方案的验证覆盖维度逐条转写（模板已含该维度） | templates/executable-design.md §5 |
| A4 | 试点 fail-fast 不破坏既有正常启动路径（现有容器已依赖真密钥） | docker/.env 现状 |

## 5. 待确认

无（Q1/Q2/Q3 已由主人 2026-09-11 拍板，见需求来源）。

## 6. 阻断项

**无。**

## 7. 专项评审触发

本仓编码 + 容器启动行为变更 → High-risk Change **未命中**（失败模式为启动即拒，比现状更安全）；Compliance/Cross-team 未命中。
命中 Business Trade-off：已由 Q1~Q3 决策闭合。

## 8. 六项覆盖检查表

| # | 检查 | 结论 | 证据 |
|---|---|---|---|
| ① 需求覆盖 | ✅ | 目标 1~7 全归 Do；排除项 5 条显式 | §1/§2/§3 |
| ② 系统覆盖 | ✅ | skills/×3 副本、architect-knowledge/、adapters/×2、docker/entrypoint.sh、docs/designs/；范围外：dsh 套件仓、dsh-yuyi、omp 镜像 | 本仓结构；v3 compose |
| ③ 证据覆盖 | ✅ | 缺口诊断源于本会话实测（工具链/挂载/流程现状）；方法依据 principle 与 practice 已确认条目 | §1 方法论依据行 |
| ④ 风险覆盖 | ✅ | 兼容：新 SKILL 纯增量；异常：fail-fast 误判需红绿实测；缓存/MQ：不适用；状态机：知识条目走待审核；安全：无权限面扩大 | §4/§7 |
| ⑤ 验证覆盖 | ✅ | G3 副本校验 + CI + lint + 试点容器红绿双态 + 主人确认；回滚=git revert+删分支 | §1 |
| ⑥ 不确定性治理 | ✅ | 无待确认；Q1~Q3 已闭合；试点结果未知部分在走查记录显式登记 | §5 |

## 9. 五问准入

| # | 问题 | 结论 |
|---|---|---|
| 1 | 问题与方案匹配？ | 问题找方案：主人明确要求编码能力；缺口由本会话实测得出（通用编码素质在、流程纪律缺失） |
| 2 | 价值依据？ | 「稳定流程进 SKILL」是本体系核心主张；编码是架构师职能的自然延伸（方案→落地闭环） |
| 3 | 范围受控？ | 一 SKILL + 一模式扩展 + 一条知识 + 两适配 + 一试点，五条不做项 |
| 4 | 可复用既有能力？ | 六维度/验证覆盖模板、G3 校验、lint、适配五面、评审骨架全部 Reuse |
| 5 | 可逆/可回滚？ | 纯文件增量 + 一次小改造；git revert/删分支即回滚 |

## 10. 准入结论

**通过，可进入 architect-design。** 交接物：本需求包；无待确认项；方案期需在走查记录登记试点实测结果。
