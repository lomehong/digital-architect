# 走查记录：知识校验面双宿主落地（architect_lint）

> 走查人：本建设会话 · 日期：2026-09-11 · 任务：TB-1789108198658-g62vu（主人已批准）
> 动因：主人指出 R9 改在 dsh 插件里，「对 omp 无效」——需要回答「能跑 / 会跑 / 被强制」三层。

## 一、落地内容

| 层 | 落地 | 说明 |
|---|---|---|
| ① 纯函数（单一事实源） | `dsh-architect/src/lint.ts`（R1~R9） | 不动；两宿主共用 |
| ② **采集**唯一实现 | **新 `src/kbcollect.ts`**（`collectKnowledgeSnapshot` / `makeRefExists` / `lintKnowledgeAt`） | **CLI 与两宿主工具共用同一采集实现**，杜绝「双采集器漂移」 |
| ③ 调用外壳（双宿主同构） | 第 4 个工具 **`architect_lint`**：dsh 侧 `src/tools.ts`（cordis 注册）+ omp 侧 `src/omp.ts`（CustomToolFactory） | 同名同语义；omp 侧另有 bash 兜底命令写入工具描述 |
| ④ 纪律（会跑） | `knowledge-distill`（落库后必跑）、`architect-implement`（涉知识库改动提交前必跑）、`architect-review`（评审复跑抽查） | 三副本同步过 G3 |
| ⑤ 适配层 | `adapters/dsh.md`、`adapters/oh-my-pi.md` 检查工具面补 `architect_lint` | CustomToolFactory ×3 → ×4 |
| ⑥ 强制 | CI（仓库级、宿主无关）+ SKILL 指令 | omp 无账本可拦，以「工具 + SKILL + CI」三件替代 |

## 二、验证结果（真实输出）

| # | 项 | 结果 |
|---|---|---|
| 1 | 单元测试 | ✅ **55/55 全绿**（原 51 + 新增 lint-tool 4 例：dsh 绿/红、omp 绿/降级） |
| 2 | 夹具教训 | 首次「干净库」用例失败——因夹具漏建 4 个知识类目录触发 R8「目录缺失」：**校验器行为正确，夹具错**（已修并记录） |
| 3 | 容器工具面 | ✅ 实测出现 `…, architect_digest, architect_design, architect_review, architect_lint`（活挂载即时生效，无需重建） |
| 4 | 容器绿态 | ✅ `kb_root=/opt/architect/architect-knowledge` → 通过（26 条 0 错） |
| 5 | 容器红态 | ✅ 临时副本注入 `D:\work\proj` → `⛔ [R9] principle/host-neutral-core.md：含设备特定路径…：D:\work\proj`，工具返回 pass=false（副本另报 8 条 R7 属拷贝导致的假阳性） |
| 6 | 副本一致性 | ✅ 5 SKILL × 2 镜像一致 |

## 三、过程发现

- **F1（架构性，待主人决策）**：主人随后指示「omp 侧应有一套完整插件承接」→ 本任务交付的 omp 工具目前仍住在 `dsh-architect/src/omp.ts`（dsh 宿主插件的 omp 导出）。若建独立仓 `omp-architect`，该工具及后续 omp 专属能力应迁入，形成「一个宿主一个实现仓」的对称结构（与决策 D10 的仓边界同源）。
- **F2（采集抽取）**：本次把采集逻辑从 CLI 抽成 `kbcollect.ts` 并让 CLI 反向复用——这是「单一事实源」纪律在**检查器内部**的一次落实（此前 CLI 与工具若各写一份采集，必然漂移）。

## 四、未兑现 / 待办

- 待主人确认本任务落定；
- F1 的仓结构决策与迁移，见新立项的 omp 插件化任务（TB-1789108205507-kbsoa）。
