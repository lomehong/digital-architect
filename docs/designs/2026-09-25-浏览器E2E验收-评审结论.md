# 评审结论：架构师 Agent 真实浏览器端到端验收能力（jev-ultrafast + 本地 Laya 全自托管栈）

- 评审对象：`docs/designs/2026-09-25-浏览器E2E验收-技术方案.md`（需求包：同日 `-需求包.md`）
- 评审方式：独立评审实例，未参与设计会话；证据回源亲自抽查（复跑 pytest、核对套件 JSON、读 model.py/agent.py 源码、逐条核对知识库行号）
- 评审日期：2026-09-25

- **结论：驳回**——评分达标（56/60、无维度 ≤5、五问全答），方案本身无需退回返工；驳回唯一原因是**决策门未清**：B1（合规）、Q3、Q4、Q5 四项 Human Decision 均无主人决策记录。按评审规则「Human Decision 未决 → 驳回」，待主人逐项拍板后可复核通过，落定（拆任务执行）前必须全部清除。
- **五问得分：Q1 2/2，Q2 2/2，Q3 2/2，Q4 2/2，Q5 2/2（10/10，全答）**
- **六维度得分：56/60**（逐维度见下）
- 本评审不是通过门，最终落定以主人确认为准。

## 五问明细

| 问 | 分 | 评注 |
|---|---|---|
| Q1 改哪里 | 2 | 精确到目录/文件级：新增 `browser-acceptance/`、改 `skills/architect-implement/SKILL.md`、改 `adapters/oh-my-pi.md`、增 `adapters/kimi-code.md`（登记不新建）、practice 回灌；核心/observatory/dsh-suite 显式不动 |
| Q2 为什么改 | 2 | 回溯到主人 2026-09-25 指令与验证覆盖缺口；§0.1 对照 reference 地图逐项 Build/Reuse 判定 |
| Q3 影响谁 | 2 | 影响面含执行面/适配层/目标项目验收环节；缺席降级=退回 bash 单测验收，不阻塞流程 |
| Q4 如何验证 | 2 | 单测/契约/回归/监控/回滚五类均可执行；评审已亲自复跑单测（31 passed）与套件结果核对 |
| Q5 还有什么没确认 | 2 | B1 + Q1–Q5 + U7/U8 显式登记，未见擅自补全；登记未知得分 |

## 六维度明细

| 维度 | 分 | 评注 |
|---|---|---|
| 需求覆盖 | 10 | Do 6 条 / Don't 6 条 / To Confirm 5 条三列齐全，逐条对齐需求包条目号，一致 |
| 系统覆盖 | 9 | 对照 `reference/dsh-suite-architecture-map.md` 无遗漏声明成立（地图 L63 纪律引用正确）；扣分点：Q2 能力归属未定（五层模型经核对确无现成层，Unknown 登记成立），且「13 技能扫描零命中」表述不严谨（见缺口 1） |
| 证据覆盖 | 8 | 关键结论抽查**全部回源通过**（见下「证据抽查记录」）；扣分点：两处表述与实证字面不符（「零命中」「两行补丁」，见缺口 1/2），需修订措辞而非结论 |
| 风险覆盖 | 9 | 兼容/异常/缓存/状态机/安全逐类有结论，MQ 不适用判定合理；写路径隔离的执行机制目前只有「运行器启动前显式声明」的纪律性设计，具体强制手段待实现期兑现 |
| 验证覆盖 | 10 | 验证手段与 §7 任务拆解可对应；评审亲自复跑 `uv run pytest` = 31 passed，suite-r3.json 逐条核对与 §3 一致 |
| 不确定性治理 | 10 | B1/Q1–Q5（Human Decision/Unknown）+ U7/U8（中文页面准确率、DAgger 二轮微调）全部显式登记，处置路径明确 |

## 证据抽查记录（亲自回源，当前行为以代码为准）

| # | 抽查项 | 方法 | 结果 |
|---|---|---|---|
| 1 | jev 离线测试 31/31 | `cd spike/jev-laya/jev-ultrafast && uv run pytest` | **属实**：31 passed in 0.19s |
| 2 | 套件基线 15/21、失败类归属 | 读 `suite-r3.json` 逐条计数 | **属实**：21 条记录 15 pass；books-page2 0/3、arxiv-search 0/3（含 1 次 error:StalePage，佐证方案 §4「基础设施 flake」表述）；其余 5 任务 3/3 |
| 3 | 「模型输出不成为选择器/JS」「DONE 不直接执行」 | 读 `jev_ultrafast/model.py`（validate_choice/choose）与 `agent.py:93-100` | **属实**：validate_choice 强制 choice∈合法 id 集、概率和≈1、置信度∈[0,1]，非法即 raise「no action executed」；post_json 错误直接 raise 不静默；DONE/BLOCKED 在 agent.py 只改 status 不执行动作，且执行前先 fresh 检查（StalePage 重决）；动作 id 全部来自 action_space 预注册映射，模型只选索引 |
| 4 | 知识库行号引用 | 逐条 sed/grep 核对 | **全部命中**：suite-build-lessons:36（dsh-browser 采集桥）/:41（pid 登记）/:42（写操作隔离夹具）/:44（常驻=木马红线）；evidence-strength-vs-conclusion:27-29（截图级证据不定性）；observatory-platform-bootstrap:50-56（含写操作与生产隔离）；task-and-memory-surface:38-42（confirm 只能主人发起）；omp-extension-contract-pitfalls:63-68（升级复跑入口）；coding-delegation:16-20（主人点名执行方）；dsh-suite-architecture-map L63（未核成员纪律）；key-decisions D9/D11/D12；五层模型在 `principle/agent-runtime-five-layers.md`，确无浏览器验收层 |
| 5 | 「体系无浏览器能力」 | Grep skills/ 全目录与 adapters/ | **方向属实、字面不实**：adapters/ 零命中为真；但 skills/ 有 1649 处 browser 命中，其中 knowledge-distill 的采集桥（dsh-browser）与 archify 的 visual-check 均为浏览器使用——均非「对目标系统的验收执行」，结论成立，但「13 个 SKILL.md 全量扫描零命中」表述错误且与方案 §0.1（承认 dsh-browser 存在）自相矛盾 |
| 6 | 「pin commit + 两行端点补丁」 | 读 `laya-browser/code/jev-ultrafast.patch` | **不完整**：model.py 实际两处 hunk——choose() 端点/key 改两行（属实）+ field_text() 新增 TEXT_MODEL_EXTRA_JSON 两行（方案未提）；另有 uv.lock 镜像源改动。补丁面被低估 |

## 缺口清单（可执行修改指引）

1. **修订「零命中」表述**（方案 §3 证据表第一行、§0.1）：改为「13 个技能无浏览器**验收执行**能力；knowledge-distill 的 dsh-browser 采集桥与 archify 的 visual-check 为浏览器他用，已在 §0.1 判不可复用」。当前表述与 §0.1 自相矛盾。
2. **补全补丁面描述**（方案 §1 Do-1、§4 兼容行）：「两行端点补丁」实为 model.py 两处 hunk（TYPESAFE_BASE_URL/KEY 容错 + TEXT_MODEL_EXTRA_JSON）及 uv.lock 镜像改动；pin 时应以补丁文件整体为准，避免升级比对漏项。
3. **写路径隔离的强制机制具体化**（方案 §4 安全①、§5）：「运行器启动前显式声明变更对象」目前只是纪律描述，建议实现期落地为运行器参数（目标基址白名单/环境标记），并在契约测试中断言。
4. **决策门清除**（落定前必须完成，见下）。

## 知识漂移登记

- 无知识库与代码冲突：抽查的 12 处知识库引用全部与现行内容一致，无需回写。
- 方案自身两处表述与实证不符（缺口 1/2），属方案修订项，不触发知识库回写。

## 待主人确认事项（决策门待决清单）

| # | 事项 | 类型 | 说明 |
|---|---|---|---|
| B1 | jev-ultrafast（MIT）+ Laya（Apache-2.0）+ cklxx/laya-browser 权重进体系供应链 | Human Decision（合规） | **落定执行的硬前置**；需求包已明确不阻塞起草与评审 |
| Q1 | Kimi Code 宿主适配缺口（决策门/任务面/记忆面映射） | Unknown | 本次以会话内文本提问兜底 |
| Q2 | 「浏览器验收」能力在五层模型中的归属 | Unknown | 经核对确无现成层，属新增能力面 |
| Q3 | 与 dsh-browser 采集桥并存 or 收敛 | Human Decision（业务取舍） | 方案按并存分工起草 |
| Q4 | CPU 本地 vs GPU 容器部署 | Human Decision（业务取舍） | 方案按 CPU 先行起草 |
| Q5 | 执行方点名（自编/委派） | Human Decision | 落定拆任务时点名 |

> 以上清除后本评审可复核通过；评审通过 ≠ 完成，落定仍以主人确认为准。

---

## 复核记录（2026-09-25）

- **复核结论：通过**——初评驳回的唯一原因（四项 Human Decision 未决）已消除，三处缺口修订经逐项核对属实。复核通过 ≠ 落定，落定以主人确认为准（四项决策主人已在 2026-09-25 会话逐项确认）。

### 决策门核对

| 门 | §8 记录 | 核对结论 |
|---|---|---|
| B1 合规 | 批准引入；执行侧落实 pin/hash/safetensors/来源登记（主人 / 2026-09-25） | 属实，需求包阻断项同步划销 |
| Q3 业务取舍 | 并存分工：采集归 dsh-browser，验收归本能力（主人 / 2026-09-25） | 属实，需求包同步 |
| Q4 业务取舍 | CPU 本地先行，GPU 为后续优化项（主人 / 2026-09-25） | 属实，需求包同步 |
| Q5 执行方 | 主人授权点名权，本会话自编（主人 / 2026-09-25） | 属实，§7 注明授权出处与理由 |
| Q1/Q2 Unknown | 主人未指示，维持登记 | 处置正确：Unknown 不擅自补全，持续登记；§7 附宿主任务面兜底说明（不发明新基建） |

### 三处缺口修订核对（逐项属实）

1. **「零命中」措辞**（§0.1 Build 行、§3 证据表首行）：已改为「13 个技能逐项核查：knowledge-distill 仅引用外部采集桥 dsh-browser、archify 仅有截图类证据读取，均非验收执行手段；adapters/ 工具面零命中」——与评审抽查结论一致，不再自相矛盾。属实。
2. **补丁面**（§1 Do#1、§3 证据表第二行）：已改为「上游补丁文件 `jev-ultrafast.patch` 整体（model.py 两处：TYPESAFE_BASE_URL 端点与密钥容错、TEXT_MODEL_EXTRA_JSON；uv.lock 镜像改动不采纳）；pin 以补丁文件为准」——与评审读 patch 原文的结论（两处 hunk + uv.lock 改动）完全对齐，且明确了 uv.lock 镜像改动不采纳的处置。属实。
3. **写路径隔离硬化**（§4 Security ①、§7 BE2E-1 验收条目）：已从纪律描述改为「运行器要求显式 `--allow-host` 白名单参数，无白名单拒绝启动，启动时打印变更对象声明」，并落入 BE2E-1 可验收条目（无白名单拒绝启动 + 打印声明）。属实，且可执行可验证。

### 残留事项（不阻塞，实现期顺手清理）

- 方案内部措辞未完全同步：§0 五问速答 Q5 行、§1 To Confirm 表、§6 不确定性治理表仍写 B1/Q3/Q4/Q5「待拍板/待确认」，与 §8 决策门记录及需求包已决策状态不一致。权威记录以 §8 + 需求包为准，建议落定前将 §6 表状态列同步为「已决策（见 §8）」。

### 复核后状态

- 五问 10/10、六维度 56/60 维持（初评扣分项 1/2/3 已修复，未重新评分——复核只核对修订属实性与决策门完整性）；
- 全部 Human Decision 有主人决策记录，Unknown（Q1/Q2/U7/U8）登记处置合规；
- **方案具备落定条件**：可按 §7 任务拆解（BE2E-1～5）进入执行，执行语义不变（自报 ≠ 完成，主人 confirm 才落定）。
